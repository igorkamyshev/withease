{
  "name": "website",
  "private": true,
  "scripts": {
    "prepare": "node scripts/changelog.mjs && node scripts/jsdoc.mjs",
    "dev": "pnpm prepare && vitepress dev docs",
    "build": "pnpm prepare && vitepress build docs"
  },
  "dependencies": {
    "@withease/web-api": "workspace:*"
  }
}
